root_cause_category: memory_unavailable

required_keywords:
- memory
- backend
- fallback
- unreachable

forbidden_categories:
- user_error
- unknown
- healthy

required_evidence_sources:
- hermes_memory_state
- hermes_session_log

optimal_trajectory:
- get_hermes_memory_state
- get_hermes_session_log

max_investigation_loops: 4

model_response: |
  Hermes external memory backend became unreachable during runtime.

  The session silently fell back to in-process memory, causing the
  agent to lose long-term conversational context across requests.

  Root cause category: memory_unavailable.
